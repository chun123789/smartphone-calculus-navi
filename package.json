{
  "name": "smartphone-calculus-navi",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:content && vite",
    "build:content": "node scripts/build-content.mjs",
    "validate:plan": "node scripts/content/validate-plan.mjs",
    "sync:articles": "node scripts/content/generate-articles.mjs",
    "build": "npm run build:content && vite build",
    "preview": "npm run build && vite preview",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "d3": "^7.9.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "sanitize-html": "^2.13.1"
  },
  "devDependencies": {
    "eslint": "^9.21.0",
    "jest": "^29.7.0",
    "vite": "^6.2.0"
  }
}
